One-sentence: single-line field with @ and send at the trailing edge; the track panel uses the shorter placeholder "Escreva no canal...".

```jsx
<Composer onSend={send} />
<Composer placeholder="Escreva no canal..." mention={false} />
```
