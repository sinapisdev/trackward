One-sentence: every view opens with this; the subtitle is a plain sentence, the actions are at most one lime button plus outlines.

```jsx
<PageHeader kicker="Sua fila" title="Meu trabalho" subtitle="5 pendências. Um próximo passo de cada vez."
  actions={<><Button variant="secondary" iconLeft={<Icon name="filter" />}>Filtrar</Button><Button iconLeft={<Icon name="plus" />}>Tarefa</Button></>} />
```
