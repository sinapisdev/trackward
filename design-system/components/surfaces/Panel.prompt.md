One-sentence: the right column ("Radar da operação", "Nesta volta", "Conversa da track") is a `rail` panel; the task sheet that floats over Meu trabalho is a `drawer`.

```jsx
<Panel variant="rail" title="Radar da operação" actions={<IconButton label="Mais"><Icon name="more-horizontal" /></IconButton>}>…</Panel>
<Panel variant="drawer">…</Panel>
```
