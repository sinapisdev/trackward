One-sentence: separates the stacked blocks of a page; sections are divided by hairlines and space, never by boxes.

```jsx
<SectionHeader title="Aguardando você" count={5} action={<Button variant="ghost" size="sm" iconRight={<Icon name="arrow-right" size={16} />}>Ver tudo</Button>} />
```
