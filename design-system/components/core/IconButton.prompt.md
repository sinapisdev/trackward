One-sentence: icon-only control for the top bar (people, settings), row overflow (`more-horizontal`) and panel close.

```jsx
<IconButton label="Ajustes" active><Icon name="settings" /></IconButton>
<IconButton label="Mais ações"><Icon name="more-horizontal" /></IconButton>
```

36px default; on mobile bump to 44px to meet the tap minimum (--tap-min).
