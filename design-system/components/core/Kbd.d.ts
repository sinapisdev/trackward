import type * as React from "react";

/** Keyboard hint capsule — the "/" affordance inside the global search field. */
export interface KbdProps { children: React.ReactNode; style?: React.CSSProperties }
export declare function Kbd(props: KbdProps): React.JSX.Element;
