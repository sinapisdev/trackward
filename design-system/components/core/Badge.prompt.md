One-sentence: a count, never a label — "Meu trabalho 5", "Aguardando você 5", "Propostas da IA 3".

```jsx
<Badge>5</Badge>
<Badge tone="accent">3</Badge>
```

Counts are plain integers with no "+" unless truncating a list ("+3" in an AvatarGroup).
