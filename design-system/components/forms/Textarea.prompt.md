One-sentence: used for "Só passa quando" criteria and descriptions; 3 rows default.

```jsx
<Field label="Só passa quando"><Textarea defaultValue="Dados de clientes e fornecedores conferidos." /></Field>
```
