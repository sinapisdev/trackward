One-sentence: the canonical work-state label; keep the exact pt-BR strings so the vocabulary stays learnable.

```jsx
<StatusPill status="late" />
<StatusPill status="blocked" variant="chip" label="Aguardando dependência" />
<StatusPill status="soon" />
```

Vocabulary: Atrasado / Travado / Vence em breve / Em dia / Concluída / A fazer / Pendente. Feminine forms appear when the subject is a tarefa or rotina ("Atrasada").
